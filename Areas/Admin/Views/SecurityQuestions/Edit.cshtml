@model IEnumerable<MasterApplication.Areas.Admin.Models.SecurityQuestionsModel>

@{ ViewData["Title"] = "SecurityQuestions Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml"; }

<div class="breadcrumb-header justify-content-between">
    <div>
        <h4 class="content-title mb-2">Edit Question</h4>
    </div>
</div>

<div class="row">
    <div class="col-md-12 col-lg-12">
        <div class="card">
            <div class="card-body ">
                <div class="pd-5 pd-sm-5 bg-gray-100">
                    <form asp-area="Admin" asp-action="Edit" asp-controller="SecurityQuestions" class="form-horizontal">
                        <div class="row">
                            <input type="hidden" asp-for="@Model.First().CODE" />
                            <div class="form-group col-md-4">
                                <label class="control-label">Security Question</label>
                                <input asp-for="@Model.First().Question" class="form-control form-control-sm mandatory" />
                            </div>
                        </div>
                        <div class="row">
                            <div class="form-group col-md-2">
                                @*<div class="form-check  ">*@
                                    @Html.CheckBoxFor(m => m.First().LOCKED)
                                    <label class="form-check-label">Locked</label>
                                @*</div>*@
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-4">
                                <div class="bd-example">
                                    <button type="submit" id="btnupdate" onclick="return CheckMandatory();" class="btn btn-outline-primary btn-sm mb-1">
                                        <i class="fe fe-check me-2"></i>Update
                                    </button>
                                    <a class="btn btn-outline-danger btn-sm mb-1" href="/Admin/SecurityQuestions/Index"><i class="fe fe-arrow-left me-2"></i>Back</a>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>